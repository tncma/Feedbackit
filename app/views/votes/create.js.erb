
$("#vote-feedback").html("<%= escape_javascript render('votes/votes', type: @votable.class.to_s, id: @votable.id) %>");
$("#vote-count-feedback").html("<%= escape_javascript pluralize @votable.votes.count, 'like' %>");